<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Boy Holding RIALO Sign</title>
  <style>
    body {
      background: #f0f4f8;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    svg {
      width: 300px;
      height: 400px;
    }
    .boy {
      fill: #6fa8dc;
      stroke: #333;
      stroke-width: 2px;
    }
    .skin {
      fill: #f5cba7;
      stroke: #333;
      stroke-width: 2px;
    }
    .sign {
      fill: #ffffff;
      stroke: #333;
      stroke-width: 2px;
    }
    .sign-text {
      font-family: Arial, sans-serif;
      font-size: 20px;
      font-weight: bold;
      fill: #333;
      text-anchor: middle;
      dominant-baseline: middle;
    }
  </style>
</head>
<body>
  <svg viewBox="0 0 200 300">
    <!-- Signboard -->
    <rect x="60" y="20" width="80" height="40" class="sign"/>
    <text x="100" y="45" class="sign-text">RIALO</text>

    <!-- Arms holding sign -->
    <line x1="70" y1="60" x2="80" y2="120" stroke="#333" stroke-width="4"/>
    <line x1="130" y1="60" x2="120" y2="120" stroke="#333" stroke-width="4"/>

    <!-- Body -->
    <rect x="85" y="120" width="30" height="60" class="boy" rx="5"/>

    <!-- Legs -->
    <line x1="95" y1="180" x2="90" y2="240" stroke="#333" stroke-width="4"/>
    <line x1="115" y1="180" x2="120" y2="240" stroke="#333" stroke-width="4"/>

    <!-- Head -->
    <circle cx="100" cy="95" r="20" class="skin"/>

    <!-- Eyes -->
    <circle cx="93" cy="90" r="2" fill="#333"/>
    <circle cx="107" cy="90" r="2" fill="#333"/>

    <!-- Smile -->
    <path d="M92 100 Q100 110 108 100" stroke="#333" stroke-width="2" fill="transparent"/>
  </svg>
</body>
</html>